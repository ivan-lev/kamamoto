{
  "scripts": {
    "dev": "concurrently \"cd front && npm run dev\" \"cd back && npm run dev\" ",
    "upload": "rsync -zarvh -e 'ssh -p <port>' --exclude-from='rsync-exclude.txt' * <user>@<server>:~/<folder>/",
    "upload-all": "rsync -zarvh -e 'ssh -p <port>' --exclude-from='rsync-all.txt' * <user>@<server>:~/<folder>/"
  },
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}
